/* eslint-disable no-unused-vars */
export enum ENVIRONMENT {
  DEV = 'development',
  PROD = 'production',
  TEST = 'test',
}

export enum PROCESS_TERMINATION_SIGNAL {
  SIGINT = 'SIGINT',
  SIGTERM = 'SIGTERM',
}
